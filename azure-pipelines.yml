# HTML
# Archive your static HTML project and save it with the build record.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest
  
variables:
  param1: 
- group: my-vars
  
parameters:
  - name: projectName
    displayName: project name?
    type: string
    

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
 
  
#- task: Bash@3
  #inputs:
    #filePath: '/Test2.sh'
- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(build.sourcesDirectory)'
    includeRootFolder: false
- task: PublishBuildArtifacts@1
- script: echo "hello to my first Build"
#- task: ShellScript@2
  #inputs:
    #scriptPath: ./Test1.sh
    #failOnStderr: true
- task: Bash@3
  inputs:
    targetType: filePath
    #arguments: ' -r eastus2 -s $(parameters.projectName)'
    filePath: ./Test1.sh
    failOnStderr: true
