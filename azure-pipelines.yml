parameters:
- name: action
  displayName: Pool action
  type: string
  default: ubuntu-latest
  values:
  - build
  - docker
  - deploy

trigger: none

jobs:
- job: build
  displayName: build
  pool: 
    vmImage: ${{ parameters.action }}
  steps:
  - script: echo The image parameter is ${{ parameters.action }}```
  - script: echo building $(parameters.action)
  - ${{ if eq(parameters.Build, true) }}:
    - script: echo "Running all the tests"



